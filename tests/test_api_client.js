import { render_api_client } from '../src/api_client.js';

test('renders api_client properly 0', () => {
    expect(render_api_client()).toContain('api_client');
});

test('renders api_client properly 1', () => {
    expect(render_api_client()).toContain('api_client');
});

test('renders api_client properly 2', () => {
    expect(render_api_client()).toContain('api_client');
});

test('renders api_client properly 3', () => {
    expect(render_api_client()).toContain('api_client');
});

test('renders api_client properly 4', () => {
    expect(render_api_client()).toContain('api_client');
});

