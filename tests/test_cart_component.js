import { render_cart_component } from '../src/cart_component.js';

test('renders cart_component properly 0', () => {
    expect(render_cart_component()).toContain('cart_component');
});

test('renders cart_component properly 1', () => {
    expect(render_cart_component()).toContain('cart_component');
});

test('renders cart_component properly 2', () => {
    expect(render_cart_component()).toContain('cart_component');
});

test('renders cart_component properly 3', () => {
    expect(render_cart_component()).toContain('cart_component');
});

test('renders cart_component properly 4', () => {
    expect(render_cart_component()).toContain('cart_component');
});

