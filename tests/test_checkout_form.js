import { render_checkout_form } from '../src/checkout_form.js';

test('renders checkout_form properly 0', () => {
    expect(render_checkout_form()).toContain('checkout_form');
});

test('renders checkout_form properly 1', () => {
    expect(render_checkout_form()).toContain('checkout_form');
});

test('renders checkout_form properly 2', () => {
    expect(render_checkout_form()).toContain('checkout_form');
});

test('renders checkout_form properly 3', () => {
    expect(render_checkout_form()).toContain('checkout_form');
});

test('renders checkout_form properly 4', () => {
    expect(render_checkout_form()).toContain('checkout_form');
});

