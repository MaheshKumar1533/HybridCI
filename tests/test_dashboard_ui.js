import { render_dashboard_ui } from '../src/dashboard_ui.js';

test('renders dashboard_ui properly 0', () => {
    expect(render_dashboard_ui()).toContain('dashboard_ui');
});

test('renders dashboard_ui properly 1', () => {
    expect(render_dashboard_ui()).toContain('dashboard_ui');
});

test('renders dashboard_ui properly 2', () => {
    expect(render_dashboard_ui()).toContain('dashboard_ui');
});

test('renders dashboard_ui properly 3', () => {
    expect(render_dashboard_ui()).toContain('dashboard_ui');
});

test('renders dashboard_ui properly 4', () => {
    expect(render_dashboard_ui()).toContain('dashboard_ui');
});

