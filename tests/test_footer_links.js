import { render_footer_links } from '../src/footer_links.js';

test('renders footer_links properly 0', () => {
    expect(render_footer_links()).toContain('footer_links');
});

test('renders footer_links properly 1', () => {
    expect(render_footer_links()).toContain('footer_links');
});

test('renders footer_links properly 2', () => {
    expect(render_footer_links()).toContain('footer_links');
});

test('renders footer_links properly 3', () => {
    expect(render_footer_links()).toContain('footer_links');
});

test('renders footer_links properly 4', () => {
    expect(render_footer_links()).toContain('footer_links');
});

