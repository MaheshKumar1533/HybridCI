import { render_modal_dialog } from '../src/modal_dialog.js';

test('renders modal_dialog properly 0', () => {
    expect(render_modal_dialog()).toContain('modal_dialog');
});

test('renders modal_dialog properly 1', () => {
    expect(render_modal_dialog()).toContain('modal_dialog');
});

test('renders modal_dialog properly 2', () => {
    expect(render_modal_dialog()).toContain('modal_dialog');
});

test('renders modal_dialog properly 3', () => {
    expect(render_modal_dialog()).toContain('modal_dialog');
});

test('renders modal_dialog properly 4', () => {
    expect(render_modal_dialog()).toContain('modal_dialog');
});

