import { render_navigation_menu } from '../src/navigation_menu.js';

test('renders navigation_menu properly 0', () => {
    expect(render_navigation_menu()).toContain('navigation_menu');
});

test('renders navigation_menu properly 1', () => {
    expect(render_navigation_menu()).toContain('navigation_menu');
});

test('renders navigation_menu properly 2', () => {
    expect(render_navigation_menu()).toContain('navigation_menu');
});

test('renders navigation_menu properly 3', () => {
    expect(render_navigation_menu()).toContain('navigation_menu');
});

test('renders navigation_menu properly 4', () => {
    expect(render_navigation_menu()).toContain('navigation_menu');
});

