import { render_product_list } from '../src/product_list.js';

test('renders product_list properly 0', () => {
    expect(render_product_list()).toContain('product_list');
});

test('renders product_list properly 1', () => {
    expect(render_product_list()).toContain('product_list');
});

test('renders product_list properly 2', () => {
    expect(render_product_list()).toContain('product_list');
});

test('renders product_list properly 3', () => {
    expect(render_product_list()).toContain('product_list');
});

test('renders product_list properly 4', () => {
    expect(render_product_list()).toContain('product_list');
});

