import { render_search_bar } from '../src/search_bar.js';

test('renders search_bar properly 0', () => {
    expect(render_search_bar()).toContain('search_bar');
});

test('renders search_bar properly 1', () => {
    expect(render_search_bar()).toContain('search_bar');
});

test('renders search_bar properly 2', () => {
    expect(render_search_bar()).toContain('search_bar');
});

test('renders search_bar properly 3', () => {
    expect(render_search_bar()).toContain('search_bar');
});

test('renders search_bar properly 4', () => {
    expect(render_search_bar()).toContain('search_bar');
});

